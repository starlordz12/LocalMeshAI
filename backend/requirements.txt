# LocalMeshAI backend dependencies
#
# Core web framework
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # multipart/form-data file uploads
pydantic>=2.6

# Geometry
trimesh>=4.0              # mesh load/analyze/export (STL/OBJ/PLY/3MF)
numpy>=1.24
manifold3d>=2.3          # watertight boolean backend (no Blender required)
networkx>=3.0            # trimesh graph operations (repair, splitting)
lxml>=4.9               # 3MF parsing
pillow>=10.0            # texture/material handling for GLB export
shapely>=2.0           # 2D ops used by some trimesh paths

# Testing
pytest>=8.0
httpx>=0.27            # FastAPI TestClient transport
